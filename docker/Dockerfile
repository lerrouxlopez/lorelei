# Placeholder Dockerfile.
# The Dockerized runtime and Reef wiring will be implemented in future milestones.

FROM debian:bookworm-slim

CMD ["sh", "-lc", "echo Lorelei Docker placeholder; sleep infinity"]

